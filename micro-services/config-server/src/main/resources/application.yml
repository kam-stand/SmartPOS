spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kam-stand/SmartPOS-configs
          default-label: dev
server:
  port: 8888